{
  "name": "postcard",
  "version": "0.1.0",
  "private": true,
  "description": "Mini digital postcards you share by link in SMS, Messenger, WhatsApp and friends — with time capsules and replies.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/server.js",
    "dev": "node --disable-warning=ExperimentalWarning --watch src/server.js",
    "test": "node --disable-warning=ExperimentalWarning --test \"test/*.test.js\""
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
